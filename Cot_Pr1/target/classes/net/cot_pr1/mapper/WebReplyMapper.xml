<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->
<mapper namespace="WebReplyMapper">
     
    <!-- 댓글 입력  문제점 중간에 삭제되면 그룹이름이 꼬임-->
    <insert id="insertReply" parameterType="net.cot_pr1.domain.WebReply" useGeneratedKeys="true" keyProperty="rnum">
		
        INSERT INTO web_reply (rnum, bnum, replytext, replyer ,regroup, restep, reindent)
        VALUES (0, #{bnum}, #{replytext}, #{replyer}, 0, 0,0)
	
		<selectKey keyProperty="rnum" resultType="int">
        SELECT LAST_INSERT_ID()
    	</selectKey>
    
    </insert>
    <!-- 그룹이름 설정하기위해...... -->
    <update id="create_setgroup" parameterType="net.cot_pr1.domain.WebReply">
    	UPDATE web_reply
        SET regroup = #{rnum}
        WHERE rnum = #{rnum}
    </update>
    
    <!-- 댓글 목록 -->
    <select id="listReply" resultType="net.cot_pr1.domain.WebReply">
        SELECT rnum, bnum, replytext, replyer, date ,regroup, restep, reindent
        FROM web_reply 
        WHERE bnum = #{bnum}
        ORDER BY regroup asc, restep asc, reindent asc
    </select>
    
       <!-- 3. 댓글 상세보기 -->
    <select id="replymodifyview" resultType="net.cot_pr1.domain.WebReply">
        SELECT rnum, bnum, replytext, replyer, date, regroup, restep, reindent FROM web_reply
        WHERE rnum = #{rnum}
    </select>
    
    
      <!-- 4. 댓글 수정 -->
    <update id="updateReply">
        UPDATE web_reply
        SET replytext = #{replytext}
        WHERE rnum = #{rnum}
    </update>
    
     <!-- 5. 댓글 삭제 -->
    <delete id="deleteReply">
        DELETE FROM web_reply
        WHERE rnum = #{rnum}
    </delete>
    
    <!-- 6. 댓글 코맨트 -->
    <insert id="insertReplyComment">
   
    	INSERT INTO web_reply (rnum, bnum, replytext, replyer ,regroup, restep , reindent)
        VALUES (0, #{bnum}, #{replytext}, #{replyer}, #{regroup} ,#{restep}+1 , #{reindent}+1) 
  
    </insert>
    
    <!-- 스탭  순서 정리하기 -->
    <update id="replyStepShape">
    
    	UPDATE web_reply
    	SET restep = restep +1
    	WHERE regroup = #{regroup} and  restep > #{restep} 
    </update>
</mapper>